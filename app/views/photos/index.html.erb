<h1>Media Photos</h1>
<p class="lead">High resolution photos of Darrell are available here. Just click on the photo you want below, and then save the Hi-Q image that opens in a new window.


    <% @photos.each do |photo| %>
<div class="low-q">
	
	<%= link_to image_tag(photo.avatar(:medium)), photo.avatar.url(:original), target: '_blank' %>
	<p class="img-caption"><%= photo.title %>
	
	
	
	 <% if current_user %><div class="edit alert" >
     <i>    <%= link_to 'Edit', edit_photo_path(photo), class: "admin-edit fa fa-edit", dismissible: true %> </i>| <%= link_to 'Destroy', photo, method: :delete, data: { confirm: 'Are you sure?' }, class: "admin-edit fa fa-trash" %></i>
		
 	</div>
<% end %>




	
</div>


    <% end %>
  
  
 
 
 <%= content_for :rh do %>

 <% if current_user %><div class="edit alert" >
  <i> <%= link_to ' New Photo ', new_photo_path, class: "admin-edit fa fa-camera-retro", dismissible: true %> </i>
 
  <button class="close" data-dismiss="alert" type="button">
      <span aria-hidden="true">×</span><span class="sr-only">Close</span>
    </button>
   
  </div>
  <% end %>
 
<%= render '/layouts/endorsement_panel.html.erb' %>
<%= render '/layouts/support_panel.html.erb' %>
<% end %>
