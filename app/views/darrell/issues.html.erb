

<h1>Darrell on the Issues</h1>

<p class="lead">Learn more about Darrell's vision for Sacramento, and the key policies he will champion as Mayor.
<p class="lead">As the campaign develops, he will be talking about more issues, so stay tuned. 
 
 
<% @issues.each do |i| %>
 
<div class="card issue-card">
	
	
		<%= image_tag i.avatar.url(:medium), class: "img-fluid"%>
		<div class="card-block">
<h4 class="card-title"><%= link_to i.title, i %></h4>
 <p class="card-text"><%= i.description %></p>
 <p><%= link_to 'Learn more', i, class: "btn btn-primary" %></p>
</div>
</div>

<% end %>






<% content_for :rh do %>
<% if current_user %><div class="edit alert" >
 <i> <%= link_to ' Add a new Issue  ', new_issue_path, class: "admin-edit fa fa-plus", dismissible: true %> </i>
 
 <button class="close" data-dismiss="alert" type="button">
     <span aria-hidden="true">×</span><span class="sr-only">Close</span>
   </button>
   
 </div>
 <% end %>
 
 <%= render '/layouts/endorsement_panel.html.erb' %>
 <%= render '/layouts/support_panel.html.erb' %>
 
 
<% end %>
